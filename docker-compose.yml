services:
  backend:
    build: ./backend
    container_name: quiz-backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      # Persist the usage logs
      - ./backend/logs/:/app/logs/

  frontend:
    build: ./frontend
    container_name: quiz-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
